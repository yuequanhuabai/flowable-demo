##############################################################################
个人想法：

Oauth2的核心對象：

1.資源擁有者：
2.客戶端：
3.授權服務器：
4.資源服務器：

前提：
客戶端已經註冊授權服務器：

用戶登錄客戶端系統；
用戶想在B客戶端系統上訪問自己在另一个A系統的數據，需要A系統進行同意授權

系统B需要提前在系统A进行注册
系统A会给系统B一个身份
当系统B访问A系统里的用户数据的时候，
1.需要A系统的用户进行授权行为，确认是用户本人的操作；
2.在系统A提供的授权页面输入账号密码，确认账号密码正确；

输入账号密码正确后，需要重定向返回系统B注册时的回调url
这个时候我有个问题，就是系统A的回调如何确认是否用户已经授权成功了？
还是说授权成功后就会调用该回调的url，
但是我还有一个问题，一般回调url会直接进入系统B已经登录后的主页；
如果是一个纯粹的回调url，系统B不会对该url进行验证吗？ 
那岂不是任意的回调url都可以进入系统B已经登录后的主页了？
该回调url有什么特征吗？ 
1.系统B如何判断识别该回调url是否为当前用户请求的回调url，需要携带当前客户端的标识吗？；
2.系统返回的页面应该也是当前用户相关的主页显示信息，如何基于回调的url来实现这个过程？

##############################################################################
个人想法：


