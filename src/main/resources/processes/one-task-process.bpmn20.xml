<?xml version="1.0" encoding="UTF-8"?>
<definitions xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:xsd="http://www.w3.org/2001/XMLSchema"
             xmlns:flowable="http://flowable.org/bpmn"
             typeLanguage="http://www.w3.org/2001/XMLSchema"
             expressionLanguage="http://www.w3.org/1999/XPath"
             targetNamespace="http://www.flowable.org/processdef">

    <process id="simpleLeaveProcess" name="Simple Leave Process" isExecutable="true">
        <!-- Start Event -->
        <startEvent id="startEvent" name="Start Leave Request"/>
        <sequenceFlow sourceRef="startEvent" targetRef="submitLeaveRequest"/>

        <!-- Submit Leave Request Task -->
        <userTask id="submitLeaveRequest" name="Submit Leave Request" flowable:assignee="${requester}">
            <documentation>Employee submits leave request with details</documentation>
        </userTask>
        <sequenceFlow sourceRef="submitLeaveRequest" targetRef="managerReview"/>

        <!-- Manager Review Task -->
        <userTask id="managerReview" name="Manager Review" flowable:assignee="manager">
            <documentation>Manager reviews and approves/rejects leave request</documentation>
        </userTask>
        <sequenceFlow sourceRef="managerReview" targetRef="decision"/>

        <!-- Decision Gateway -->
        <exclusiveGateway id="decision" name="Approval Decision"/>
        
        <!-- Approved Path -->
        <sequenceFlow sourceRef="decision" targetRef="approvedEnd" name="Approved">
            <conditionExpression xsi:type="tFormalExpression">${approved == true}</conditionExpression>
        </sequenceFlow>
        
        <!-- Rejected Path -->
        <sequenceFlow sourceRef="decision" targetRef="rejectedEnd" name="Rejected">
            <conditionExpression xsi:type="tFormalExpression">${approved == false}</conditionExpression>
        </sequenceFlow>

        <!-- End Events -->
        <endEvent id="approvedEnd" name="Leave Approved"/>
        <endEvent id="rejectedEnd" name="Leave Rejected"/>
    </process>

</definitions>